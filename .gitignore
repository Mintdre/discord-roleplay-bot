# Environment file
.env

# Go build outputs
go/elybot
elybot

# Rust build artifacts
rust/target/
rust/Cargo.lock 

# OS-specific files
.DS_Store
Thumbs.db

# Shared library if copied (though typically handled by build process)
go/libely_rust.so
go/libely_rust.dylib
go/libely_rust.dll

# Data files (user/server memory) - consider if you want to version example files but not actual data
rust/data/*_memory.json
# If you want to keep example files versioned, but ignore live data:
rust/data/*_memory.json
!rust/data/example_*.json

# Log files
*.log
elybot
libely_rust.so
